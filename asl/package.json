{
  "name": "step-function-poc",
  "version": "1.0.0",
  "description": "AWS Step Functions POC",
  "main": "dist/createStateMachine.js",
  "scripts": {
    "build": "tsc && tsc -p tsconfig.lambda.json",
    "start": "npm run build && node dist/createStateMachine.js",
    "trigger": "node dist/triggerStateMachine.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-eventbridge": "^3.0.0",
    "@aws-sdk/client-iam": "^3.0.0",
    "@aws-sdk/client-lambda": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-sfn": "^3.0.0",
    "archiver": "^5.3.1"
  },
  "devDependencies": {
    "@types/archiver": "^5.3.2",
    "@types/node": "^18.0.0",
    "typescript": "^4.9.0"
  }
}
